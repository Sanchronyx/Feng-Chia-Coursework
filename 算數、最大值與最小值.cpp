#include <stdio.h>

int main(void){

    double a = 0, b = 0, c = 0;
    scanf("%d %d %d", &a, &b, &c);
    
    double sum = 0, avg = 0, prod = 0, min = 0, max = 0;
    sum = a + b + c;
    avg = sum / 3;
    prod = a * b * c;
    min = a;
    max = a;

    if ()
}